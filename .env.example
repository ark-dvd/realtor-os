# Required for Sanity CMS
NEXT_PUBLIC_SANITY_PROJECT_ID=6hdpy5q3
NEXT_PUBLIC_SANITY_DATASET=production
SANITY_API_TOKEN=your_sanity_write_token_here

# Admin Authorization (choose one or both methods)
# Method 1: Email allowlist (comma-separated)
ADMIN_EMAILS=your-email@example.com,another-admin@example.com

# Method 2: Use Netlify Identity roles (set 'admin' role in Identity dashboard)
# No env var needed - just add role to user

# Initial Setup Only - REMOVE AFTER SEEDING
ALLOW_SEED=true

# DEV ONLY - Never use in production!
# ADMIN_ALLOW_ANY_AUTHENTICATED=true
